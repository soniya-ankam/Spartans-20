N = int(input())
for N in range(N):
    m,n = input().split()
    m,n = int(m),int(n)
    for num in range(m,n+1):
        if num > 1:
            for k in range(2, num):
                if (num % k) == 0:
                    break
            else:
                print(num)
